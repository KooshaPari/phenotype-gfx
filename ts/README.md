# @phenotype/gfx-postfx (TypeScript placeholder)

Placeholder for TypeScript post-processing effects.

If `phenotype-postfx` turns out to be primarily TypeScript, this directory becomes its
home as an npm package under the phenotype-gfx monorepo.

Current status: **stub** — awaiting fold decision (see ADR-003).
