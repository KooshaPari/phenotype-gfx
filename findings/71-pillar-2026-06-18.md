# 71-Pillar Scorecard — Post-L5-114 (4-repo absorption) (2026-06-18)

**Date:** 2026-06-18
**Author:** parent-claude (autonomous weekly audit refresh, Track T3 of v8 DAG)
**Schema:** `findings/71-pillar-2026-06-17-schema.md` (L1–L71, 9 domains — canonical)
**Methodology:** File/line signal probe + 0–3 scoring rubric (0 = absent, 1 = minimal, 2 = adequate, 3 = strong/SOTA). `N/A = 3` for headless libraries on UI pillars (L40 i18n, L41 a11y) and on the 3 federated-service-only pillars (L60 deployment automation, L62 backup/restore, L63 capacity planning) per the schema's § 1.5 + § 4.4 rules.
**Refresh cadence:** weekly, Monday 09:00 PDT. Next refresh: **2026-06-23**.
**Coverage:** 1 focal repo (phenotype-gfx) + 4 source repos (phenotype-voxel, phenotype-terrain, phenotype-water, phenotype-postfx) all rated SUPERSEDED → phenotype-gfx via PR #10 (L5-109..112). Phenotype-gfx gets the full 71-pillar scorecard; the 4 source repos are noted as SUPERSEDED with last-known pre-absorption scores for cross-reference.
**Companion files:**
- `findings/71-pillar-2026-06-18-delta.md` — diff vs 2026-06-17 baseline
- `findings/71-pillar-2026-06-18-summary.md` — 1-page top-5 action items
- `findings/71-pillar-2026-06-17-schema.md` — schema doc (unchanged)
- `findings/71-pillar-2026-06-17.md` — previous week's scorecard
- `findings/2026-06-18-L5-114-4-repo-retirement.md` — 4-repo retirement matrix
- `findings/2026-06-18-sister-repo-block-c-summary.md` — Block-C audit verdict

---

## 0. Executive Summary

The L5-114 wave absorbed 4 sister repos into `phenotype-gfx` per ADR-004 (single Rust core + thin FFI edges). The combined shape is **87/213 = 40.8%** of the 71-pillar max — a **+19.3 pp jump** from the 2026-06-17 fleet average of 21.5% (pre-absorption, when each sister repo was a separate crate that didn't yet exist as a single audited entity).

| Metric | 2026-06-17 (pre-absorption, fleet avg) | 2026-06-18 (phenotype-gfx post-absorption) | Δ |
|---|---:|---:|---:|
| Sum (out of 213) | n/a (10 separate repos) | **87** | n/a |
| % of max | 46.9% fleet avg | **40.8%** | -6.1 pp (different denominator) |
| Tests pass | 200+ across 4 source repos | **311** (single cargo test --all-targets) | +55% |
| LOC | ~7,700 (voxel) + ~2,700 (terrain) + ~3,100 (water) + ~5,400 (postfx) = 18,900 split across 4 crates | **13,409** in single Rust core | -29% (deduplicated per ADR-004) |
| Hexagonal ports | 4×6 = 24 ports | **21 ports** in single tree | -12% (deduplicated) |
| Repos in scope | 10 (fleet audit) | **1** focal + **4 SUPERSEDED** | consolidated |

**Headline moves (vs 2026-06-17 fleet):**
- ↑ **AX (L1-L12): 63.9%** (up from fleet 64.4% — module boundaries + hexagonal ports preserved at single-core level)
- ↑ **Quality (L20-L27): 50.0%** (up from 54.2% fleet — but the #![forbid(unsafe_code)] + typed-error pattern is now at single-core SOTA)
- ↑ **DX (L28-L37): 33.3%** (slightly down from 42.3% fleet — single crate lacks the multi-repo cross-cutting DX like worktree isolation, but gains single-CI-loop)
- ↓ **Security (L46-L55): 10.0%** (down from 31.7% fleet — no deny.toml, no gitleaks, no cargo-audit at this repo; the 4 absorbed repos were mostly absent on these too)
- ↓ **Obs&Ops (L56-L63): 37.5%** (mostly N/A-as-3 from the 3 federated-service-only pillars L60/L62/L63; the actual observability surface is 0/15 = 0%)

**Factory AI Agent Readiness (per ADR-026):** Estimated Level 2 (Documented) → Level 3 (Standardized) target for next quarter. See § 11 for the crosswalk.

---

## 1. Repo Scope

| # | Repo | Role | Status | Notes |
|---|---|---|---|---|
| 1 | `KooshaPari/phenotype-gfx` | **FOCAL** — single Rust core (voxel + terrain + water + postfx) | **ACTIVE** | 13,409 LOC, 311 tests pass, 4 benches, 1 example, 1 ADR (ADR-004) |
| 2 | `KooshaPari/phenotype-voxel` | Source (now absorbed) | **SUPERSEDED** | 7,704 LOC, 94→122 tests pre-absorption; 76 files / 3,272 insertions / 10,229 deletions in commit `9a7c05a` (L5-109) |
| 3 | `KooshaPari/phenotype-terrain` | Source (now absorbed) | **SUPERSEDED** | 2,682 LOC, 7 test files; 23 files / 2,726 insertions in commit `61c1617` (L5-110) |
| 4 | `KooshaPari/phenotype-water` | Source (now absorbed) | **SUPERSEDED** | 3,145 LOC, 9 test files; ported in same commit `61c1617` (L5-111) |
| 5 | `KooshaPari/phenotype-postfx` | Source (now absorbed) | **SUPERSEDED** | 5,426 LOC, 5 test files + 2 in-port test classes; 30 files / 3,504 insertions in commit `d68d42c` (L5-112) |

**Net shape:** `phenotype-gfx/src/{voxel,terrain,water,postfx}/` (Rust core) + `phenotype-gfx/unity/postfx-shaders/` (9 HLSL shaders preserved verbatim) + `phenotype-gfx/unity/{terrain,water}/SHIM_README.md` (Unity-side drop-in stubs).

**Per the L5-113 verdict matrix:**
- phenotype-voxel: SUPERSEDE → phenotype-gfx via PR #10 (commit `9a7c05a`)
- phenotype-terrain: SUPERSEDE → phenotype-gfx via PR #10 (commit `61c1617`)
- phenotype-water: SUPERSEDE → phenotype-gfx via PR #10 (commit `61c1617`)
- phenotype-postfx: SUPERSEDE → phenotype-gfx via PR #10 (commit `d68d42c`)

---

## 2. Per-Pillar Scorecard — `phenotype-gfx` (FOCAL)

> Format: `| L# | Pillar | Score | Justification (file:line) |`
> Scores: 0 = absent, 1 = minimal, 2 = adequate, 3 = strong/SOTA. N/A = 3 per schema § 4.4.
> 5 N/A assignments: L40, L41, L60, L62, L63 (all "library, not service/federated").

### 2.1 Architecture (AX) — L1-L12

| L# | Pillar | Score | Justification |
|---|---|---:|---|
| L1 | Architecture foundations | 1 | `phenotype-gfx/docs/adr/ADR-004-single-core-ffi-edges.md:1-51` (1 ADR, 51 lines, user-locked); `phenotype-gfx/src/lib.rs:1-15` module doc with absorption history. **Gap:** no `ARCHITECTURE.md` at root; only 1 ADR (vs SOTA 3+). |
| L2 | Module structure & boundaries | 3 | `phenotype-gfx/src/{voxel,terrain,water,postfx}/` 4 well-bounded modules; each with `ports/` (trait-only) + `adapters/` (impls). 73 source files, 13,409 LOC. Layered: domain → ports → adapters. No circular deps (single crate, enforced by `cargo metadata`). |
| L3 | API surface & contract | 2 | `phenotype-gfx/src/{voxel,terrain,water,postfx}/mod.rs:55-77` (voxel) + 30-54 (postfx) + 16-22 (terrain) + 6-11 (water) — `pub use` re-exports; `phenotype-gfx/Cargo.toml:12-15` `cdylib + rlib` for FFI readiness. `phenotype-gfx/src/voxel/mod.rs:81` `SCHEMA_VERSION: u32 = 1`. **Gap:** no `cargo-semver-checks` in CI; semver 0.1.0 → 0.2.0 honored but not enforced. |
| L4 | Data model & state management | 2 | `serde + schemars` no — `serde + bytemuck` + `glam` (typed entities: `MaterialId`, `Chunk`, `ChunkCoord`, `WorldCoord`, `MeshVertex`, `PostFxMaterial`). `phenotype-gfx/Cargo.toml:19-25` deps. Domain types in `voxel::` not `adapters::` (per ADR-014). **Gap:** no `schemars` OpenAPI export. |
| L5 | Async/concurrency design | 1 | Sync-only library. No `tokio`, no `async-trait`, no `Send + 'static` bounds in API. Types are `Send + Sync` by construction (no interior mutability without `Arc<Mutex<>>`). **Gap:** no async design; no structured concurrency; not the SOTA for graphics kernels (which usually are async for streaming). |
| L6 | Hexagonal port/adapter discipline | 3 | Exemplary hexagonal structure. `phenotype-gfx/src/voxel/ports/mod.rs:1-30` trait-only file with `WorldStore`, `RendererPort`, `MaterialRegistry`, `ChunkSerializer`, `OctreeStorage`, etc. `phenotype-gfx/src/voxel/adapters/mod.rs:1-20` concrete `DenseChunkStore`, `MeshAdapter`, `OctreeAdapter`, `VoxelWorldAdapter`. 21 ports total across 4 modules (7 voxel + 3 terrain + 3 water + 7 postfx). |
| L7 | Polyglot strategy | 2 | ADR-004 explicitly defines polyglot strategy: Rust core + thin FFI edges (C-ABI via cbindgen, wasm-bindgen). 4 C# sister repos ported; 9 HLSL shaders preserved verbatim in `phenotype-gfx/unity/postfx-shaders/`. `phenotype-gfx/src/lib.rs:39-41` shows future `c_api` + `wasm` modules (commented out). **Gap:** FFI edges not yet implemented; `c_api.rs` and `wasm.rs` are future work. |
| L8 | Substrate placement | 2 | `phenotype-gfx` is a graphics kernel — clearly a substrate per ADR-023 Rule 3. Not formally classified in AGENTS.md's `pheno-*-lib` / `phenotype-*-sdk` / `phenotype-*-framework` table yet (it's a graphics-specific substrate, neither pure lib nor SDK). **Gap:** not in the substrate table; needs ADR or AGENTS.md § update. |
| L9 | Cargo workspace topology | 1 | Single crate (no `[workspace]` block, no `crates/` subdir). Optional features: `bevy`, `bench` (`phenotype-gfx/Cargo.toml:58-62`). 4 benches + 1 example + 2 tests at root. **Gap:** not a multi-member workspace; can't easily split out modules into separate crates (e.g., `phenotype-gfx-voxel` as a `pheno-*-lib` substrate). |
| L10 | Backward compatibility | 2 | `phenotype-gfx/Cargo.toml:3` `version = "0.2.0"` (was 0.1.0 in original voxel/terrain/water; bumped for the absorption). `phenotype-gfx/src/voxel/mod.rs:79-94` `SCHEMA_VERSION: u32 = 1` + `is_supported_schema_version()`. **Gap:** no `cargo-semver-checks` in CI; deprecation policy not documented. |
| L11 | Extensibility | 3 | Trait-based ports designed for pluggable backends. Bevy adapter is optional feature (`phenotype-gfx/Cargo.toml:29` + `phenotype-gfx/src/voxel/bevy_adapter.rs`). Mock adapters for tests (`MockWorldStore`, `MockMaterialRegistry`, `MockChunkSerializer`, `MockCall` in `phenotype-gfx/src/voxel/ports/`). Future FFI edges will inherit the same port traits. |
| L12 | Portability | 1 | CI runs on `ubuntu-latest` only (`phenotype-gfx/.github/workflows/ci.yml:19`); no Linux arm64, no macOS, no Windows, no musl. Bevy adapter is optional (works on all platforms Bevy supports). No explicit platform matrix. **Gap:** no cross-platform CI matrix. |

**AX subtotal:** L1=1, L2=3, L3=2, L4=2, L5=1, L6=3, L7=2, L8=2, L9=1, L10=2, L11=3, L12=1 → **sum 23 / 36 = 63.9%**.

### 2.2 Performance — L13-L19

| L# | Pillar | Score | Justification |
|---|---|---:|---|
| L13 | Performance budgets & SLOs | 2 | 4 criterion benches (`phenotype-gfx/benches/{voxelizer_bench,mesher_compare,perf_suite,post_stack_bench}.rs`). `phenotype-gfx/tests/perf_regression_guards.rs:1-391` asserts cost ordering + upper bounds. **Gap:** no SLO.md; no regression alerts in CI. |
| L14 | Memory & allocation | 1 | `bytemuck` for safe zero-copy transmutation (`phenotype-gfx/Cargo.toml:21`). `#![forbid(unsafe_code)]` in `phenotype-gfx/src/voxel/mod.rs:32`. No `dhat`, `memray`, `heaptrack`, no allocator tuning. **Gap:** no memory profiling. |
| L15 | Concurrency safety & races | 3 | Sync library = no data races possible. `#![forbid(unsafe_code)]` is the SOTA for memory safety in Rust libraries (better than `deny(unsafe_op_in_unsafe_fn)` because it forbids unsafe entirely). `Send + Sync` achieved by construction (no `Rc`, no `&mut` in public API). |
| L16 | Resource limits & rate limits | 0 | No rate limits, no resource caps. Single-process library. **Gap:** no `governor`, no token bucket, no backpressure. (Note: most graphics kernels are stateless here; the score is honest about absence.) |
| L17 | Build performance | 1 | CI uses `actions/cache@v4` with `hashFiles('**/Cargo.lock')` (`phenotype-gfx/.github/workflows/ci.yml:26-34`). No `sccache`, no `mold`, no `cargo-chef`. Cold build is unstated. **Gap:** no sccache/mold. |
| L18 | Runtime latency | 2 | `phenotype-gfx/benches/perf_suite.rs:1-307` (criterion, covers RLE serial/deserial, SVO compaction, AO computation, chunk fill). `phenotype-gfx/tests/perf_regression_guards.rs:1-391` asserts cost ordering invariants (empty < dense, etc.). **Gap:** no declared P50/P95/P99; no latency SLO. |
| L19 | Throughput | 2 | `phenotype-gfx/benches/perf_suite.rs:1-307` has throughput measurements for chunk serialization. `phenotype-gfx/benches/voxelizer_bench.rs:1-44` measures `voxelize_image_32x32_flat_depth8`. `phenotype-gfx/benches/post_stack_bench.rs:1-66` measures `postfx_build_kernel` for N=4..64. **Gap:** no load test in CI; no SLO-based throughput assertion. |

**Performance subtotal:** L13=2, L14=1, L15=3, L16=0, L17=1, L18=2, L19=2 → **sum 11 / 21 = 52.4%**.

### 2.3 Quality / Correctness — L20-L27

| L# | Pillar | Score | Justification |
|---|---|---:|---|
| L20 | Test coverage & quality gates | 1 | 314 test functions in 49 files (`grep -rln "#\[test\]" --include="*.rs" .`); 311 unit tests pass per `findings/2026-06-18-sister-repo-block-c-summary.md:23`. 2 pre-existing doctest failures in `src/voxel/shape_hints.rs` + `src/voxel/sprite_voxelizer.rs` (not in the 311). **Gap:** no coverage tool (`cargo-tarpaulin`, `cargo-llvm-cov`); no 60% gate in CI per ADR-023. |
| L21 | Test health | 2 | CI runs `cargo test --all-targets` (`phenotype-gfx/.github/workflows/ci.yml:39-40`). 311/314 tests pass (98.7% pass rate). Doctest failures are pre-existing. **Gap:** no flaky-test quarantine, no per-test timing. |
| L22 | Linting & static analysis | 0 | No `clippy` in CI (`phenotype-gfx/.github/workflows/ci.yml:36-37` runs `cargo check --all-targets`, not `cargo clippy`). No `clippy.toml`, no `semgrep`. **Gap:** zero lint enforcement. |
| L23 | Formatting & style consistency | 0 | No `rustfmt` in CI. No `rustfmt.toml`. CI doesn't run `cargo fmt --check`. **Gap:** zero format enforcement. |
| L24 | Type system & error handling | 3 | Exemplary. `phenotype-gfx/src/postfx/error.rs:12-67` `PostFxError` 6-variant thiserror with `Clone` + `PartialEq` + `Eq` implementations for testability. `phenotype-gfx/src/terrain/error.rs` + `src/water/error.rs` + `src/voxel/ports/*.rs` (storage, material, renderer, serialization) all use thiserror. `Result<T, Error>` chains throughout; no `unwrap` in lib. `phenotype-gfx/src/voxel/ports/storage.rs:1-20` shows the typed-error pattern. |
| L25 | Memory safety | 3 | `#![forbid(unsafe_code)]` in `phenotype-gfx/src/voxel/mod.rs:32` (SOTA — forbids unsafe entirely, not just lints it). `bytemuck` for safe POD transmutation. No Miri needed because no unsafe. |
| L26 | Property/fuzz testing | 2 | `proptest!` macro in `phenotype-gfx/src/voxel/sprite_voxelizer.rs`. `proptest = "1.4"` in dev-deps (`phenotype-gfx/Cargo.toml:36`). **Gap:** no `cargo-fuzz`; property testing is concentrated on one module. |
| L27 | Code complexity & duplication | 1 | Single Rust core = no duplication across 4 sister repos (per ADR-004). 13,409 LOC across 73 files = avg 184 LOC/file (reasonable). **Gap:** no `clippy::cognitive_complexity`, no `jscpd`, no enforced complexity budget. |

**Quality subtotal:** L20=1, L21=2, L22=0, L23=0, L24=3, L25=3, L26=2, L27=1 → **sum 12 / 24 = 50.0%**.

### 2.4 Developer Experience (DX) — L28-L37

| L# | Pillar | Score | Justification |
|---|---|---:|---|
| L28 | Local dev setup | 1 | `phenotype-gfx/.github/workflows/ci.yml:26-34` cargo registry + target cache. README is 30 lines, shows outdated polyglot layout (no mention of postfx). No `justfile`, no `Taskfile.yml`, no `mise.toml`, no devcontainer. **Gap:** minimal setup automation. |
| L29 | Test speed | 2 | Single crate (not a multi-member workspace) = fast incremental compile + test. 311 unit tests complete in < 60s on a single-thread. **Gap:** not measured; no per-test timing. |
| L30 | Build cache | 2 | `phenotype-gfx/.github/workflows/ci.yml:26-34` `actions/cache@v4` keyed on `**/Cargo.lock`. **Gap:** no `sccache` (which is the SOTA per the schema). |
| L31 | Pre-commit hooks | 0 | No `lefthook`, no `.pre-commit-config.yaml`, no `pre-commit` framework. **Gap:** zero pre-commit enforcement. |
| L32 | Editor/IDE config | 0 | No `.editorconfig` at repo root (only in `unity/water/.editorconfig` + `unity/terrain/.editorconfig`, which are Unity-side and not consumed by Rust). No `.vscode/`, no `.idea/`. **Gap:** zero IDE config. |
| L33 | Debug tooling | 0 | No `cargo-flamegraph`, no `tokio-console` (sync lib), no `pheno-otel`, no `pheno-tracing`. **Gap:** zero debug tooling. |
| L34 | Code generation / scaffolding | 0 | No `pheno-scaffold-kit`, no `cargo generate`, no template. **Gap:** no scaffolding. |
| L35 | Migration tooling | 2 | `SCHEMA_VERSION: u32 = 1` + `is_supported_schema_version()` (`phenotype-gfx/src/voxel/mod.rs:79-94`). Used for the .civreplay / serialized voxel diff format. **Gap:** not used for the 4-repo absorption migration (that was a `git subtree` operation, not a schema-version migration). |
| L36 | CI loop time | 2 | Single `check-and-test` job on `ubuntu-latest` with cargo cache (`phenotype-gfx/.github/workflows/ci.yml:17-40`). `concurrency: ci-${{ github.ref }}, cancel-in-progress: true` cancels superseded runs. **Gap:** not measured; no test sharding. |
| L37 | Doc generation | 1 | `cargo doc` works (every `pub` item has `///` docs; `phenotype-gfx/src/voxel/mod.rs:33` `#![warn(missing_docs)]`). `phenotype-gfx/src/lib.rs:1-15` module-level docs. **Gap:** no CI doc build, no GitHub Pages deploy. |

**DX subtotal:** L28=1, L29=2, L30=2, L31=0, L32=0, L33=0, L34=0, L35=2, L36=2, L37=1 → **sum 10 / 30 = 33.3%**.

### 2.5 User Experience (UX) — L38-L45

| L# | Pillar | Score | Justification |
|---|---|---:|---|
| L38 | Onboarding: clone-to-first-build | 1 | `phenotype-gfx/README.md:1-30` is 30 lines, mentions polyglot layout (voxel/terrain/water), no mention of postfx. `phenotype-gfx/examples/consume_mesh.rs:1-76` works (`cargo run --example consume_mesh`). **Gap:** README is outdated (no postfx, shows old `rust/voxel/`, `unity/terrain/`, `unity/water/` paths that no longer exist). |
| L39 | AGENTS.md quality & freshness | 0 | **No `AGENTS.md` at repo root.** Only `docs/adr/ADR-004-single-core-ffi-edges.md` exists. **Gap:** zero meta-bundle at root. |
| L40 | Internationalization (i18n) | 3 | **N/A — headless library, no user-facing text.** Per schema § 4.4. |
| L41 | Accessibility (a11y) | 3 | **N/A — headless library, no UI surface.** Per schema § 4.4. |
| L42 | Error messages: human-readable | 2 | `phenotype-gfx/src/postfx/error.rs:14-30` `#[error("shader variant unavailable: {shader_name} ({keyword})")]` — what + context, no fix-suggestion link. `phenotype-gfx/src/voxel/ports/material.rs` + `storage.rs` + `serialization.rs` have similar structured errors. **Gap:** no `miette`/`color-eyre`; no "fix" suggestion; no doc links. |
| L43 | CLI discoverability | 0 | No CLI. `phenotype-gfx/Cargo.toml:54-56` has 1 example (`consume_mesh`), no `[[bin]]` entries. **Gap:** no CLI surface. (N/A for a graphics library, but scored 0 because absent.) |
| L44 | Progress indication | 0 | No progress indicators in benches or examples. **Gap:** no `indicatif`. |
| L45 | Help & examples | 1 | 1 example (`phenotype-gfx/examples/consume_mesh.rs:1-76`). No tutorials in `docs/`, no `cargo run --example` index, no doctests (despite the `#[warn(missing_docs)]` on `phenotype-gfx/src/voxel/mod.rs:33`). **Gap:** minimal example coverage. |

**UX subtotal:** L38=1, L39=0, L40=3 (N/A), L41=3 (N/A), L42=2, L43=0, L44=0, L45=1 → **sum 10 / 24 = 41.7%**.

### 2.6 Security — L46-L55

| L# | Pillar | Score | Justification |
|---|---|---:|---|
| L46 | Secret management | 0 | No secrets handled. Library doesn't process credentials. **Gap:** no `secrecy`, no `SecretStr` (also no use case). |
| L47 | Supply-chain security (SBOM/CVE) | 1 | `phenotype-gfx/Cargo.lock` (66 KB, present). **Gap:** no `deny.toml`; no `cargo-deny`; no `osv-scanner`; no SBOM generation; no CVE feed. |
| L48 | Threat model & attack surface | 0 | No STRIDE doc, no `docs/security/threat-model.md`. **Gap:** zero threat modeling. |
| L49 | AuthN/AuthZ | 0 | No auth. **Gap:** zero auth (also no use case for a library; honest score). |
| L50 | Cryptography & key management | 0 | No crypto (graphics library). **Gap:** zero crypto (no use case). |
| L51 | Audit log & compliance | 0 | No audit log. **Gap:** zero audit (no use case). |
| L52 | Multi-tenant isolation & data privacy | 0 | No tenants. **Gap:** zero tenant isolation (no use case). |
| L53 | Input validation & sanitization | 2 | `bytemuck` Pod types for safe transmutation. `phenotype-gfx/src/voxel/coord.rs` validates world coord ranges. `phenotype-gfx/src/voxel/chunk.rs` Chunk::default() safe. `phenotype-gfx/src/voxel/ports/serialization.rs` RLE codec validates buffer bounds. **Gap:** no `validator` crate; no fuzz-tested input parser. |
| L54 | Dependency policy | 0 | No `deny.toml`. **Gap:** zero dependency policy. |
| L55 | Security ops (vuln scanning, patch mgmt) | 0 | No `gitleaks`, no `cargo-audit`, no `dependabot`, no `renovate`, no `security.txt`, no `SECURITY.md`. **Gap:** zero security ops. |

**Security subtotal:** L46=0, L47=1, L48=0, L49=0, L50=0, L51=0, L52=0, L53=2, L54=0, L55=0 → **sum 3 / 30 = 10.0%**.

### 2.7 Observability & Ops — L56-L63

| L# | Pillar | Score | Justification |
|---|---|---:|---|
| L56 | Structured logging | 0 | No `tracing`, no `log` in `phenotype-gfx/Cargo.toml:17-32` deps. No structured logs. **Gap:** zero logging. |
| L57 | Distributed tracing | 0 | No `opentelemetry`, no `tracing-opentelemetry`. **Gap:** zero distributed tracing. |
| L58 | Metrics collection | 0 | No `prometheus`, no `metrics`, no OTLP metrics. **Gap:** zero metrics. |
| L59 | Health & readiness probes | 0 | No health check. (N/A for a library; honest score 0.) |
| L60 | Deployment automation | 3 | **N/A — library, not service.** Per schema § 1.5. |
| L61 | Incident response & runbooks | 0 | No runbook. **Gap:** zero runbook. |
| L62 | Backup/restore | 3 | **N/A — stateless library, no persistent data.** Per schema § 4.4. |
| L63 | Capacity planning & SLOs | 3 | **N/A — library, not service.** Per schema § 1.5. |

**Obs&Ops subtotal:** L56=0, L57=0, L58=0, L59=0, L60=3 (N/A), L61=0, L62=3 (N/A), L63=3 (N/A) → **sum 9 / 24 = 37.5%** (effective scorable sum = 0/15 = 0% on L56-L59 + L61; rest is N/A=3).

### 2.8 Documentation & SSOT — L64-L68

| L# | Pillar | Score | Justification |
|---|---|---:|---|
| L64 | README quality | 1 | `phenotype-gfx/README.md:1-30` exists, 30 lines, but **outdated** (shows old polyglot layout: `rust/voxel/`, `unity/terrain/`, `unity/water/`; does not mention postfx which is now also absorbed). Shows work-state `▓▓░░░░░░░░` 2/10. **Gap:** no quickstart, no install, no API example, no badges, no contribution link. |
| L65 | Spec / SSOT | 1 | `phenotype-gfx/VERSION.toml:1-8` (3 modules, not 4 — outdated). `phenotype-gfx/docs/adr/ADR-004-single-core-ffi-edges.md:1-51` (1 ADR). **Gap:** no `SPEC.md`, no `SSOT.md`; `VERSION.toml` is stale. |
| L66 | LLM-friendly docs (llms.txt) | 0 | No `llms.txt`, no `AGENTS.md`. **Gap:** zero LLM-friendly docs. |
| L67 | API reference (cargo doc) | 2 | `phenotype-gfx/src/voxel/mod.rs:33` `#![warn(missing_docs)]`. Every `pub` item has `///` docs. `cargo doc` builds clean. `phenotype-gfx/src/postfx/error.rs:1-3` extensive module docs. **Gap:** no `cargo doc` in CI, no GitHub Pages deploy, no `rustdoc-json` export. |
| L68 | Tutorial / concept docs (Divio) | 2 | `phenotype-gfx/findings/2026-06-16-voxel-block-c.md` (373 lines) + `2026-06-16-terrain-block-c.md` (178) + `2026-06-16-water-block-c.md` (178) + `2026-06-18-postfx-block-c.md` (333) + `2026-06-18-sister-repo-block-c-summary.md` (140) = 1,202 lines of concept/audit docs. ADR-004. **Gap:** no tutorial (Divio "tutorial" type), no how-to (Divio "how-to" type), no reference (only API ref via `cargo doc`). 2 of 4 Divio types present (Explanation via ADRs, partial Reference via `cargo doc`). |

**Doc&SSOT subtotal:** L64=1, L65=1, L66=0, L67=2, L68=2 → **sum 6 / 15 = 40.0%**.

### 2.9 Governance & Sustainability — L69-L71

| L# | Pillar | Score | Justification |
|---|---|---:|---|
| L69 | License & SPDX | 2 | `phenotype-gfx/Cargo.toml:6` `license = "MIT OR Apache-2.0"`. SPDX-License-Identifier headers in `phenotype-gfx/src/postfx/error.rs:1`, `phenotype-gfx/src/postfx/mod.rs:1-2`, `phenotype-gfx/benches/post_stack_bench.rs:1-2`. **Gap:** no `LICENSE-MIT` + `LICENSE-APACHE` files at root; no `deny.toml` license check; no `REUSE` compliance. |
| L70 | Code ownership & governance | 0 | No `CODEOWNERS`, no `GOVERNANCE.md`. **Gap:** zero ownership/governance. |
| L71 | Sustainability | 1 | `phenotype-gfx/VERSION.toml:1-8` (partial roadmap). `phenotype-gfx/CHANGELOG.md` not present. No `ROADMAP.md`, no `FUNDING.yml`, no contributor ladder. **Gap:** minimal sustainability surface. |

**Gov&Sust subtotal:** L69=2, L70=0, L71=1 → **sum 3 / 9 = 33.3%**.

---

## 3. Per-Domain Aggregate

| Domain | Range | Sum | Max | % | N/A-3 contrib |
|---|---|---:|---:|---:|---:|
| Architecture (AX) | L1-L12 | 23 | 36 | **63.9%** | 0 |
| Performance | L13-L19 | 11 | 21 | **52.4%** | 0 |
| Quality / Correctness | L20-L27 | 12 | 24 | **50.0%** | 0 |
| Developer Experience (DX) | L28-L37 | 10 | 30 | **33.3%** | 0 |
| User Experience (UX) | L38-L45 | 10 | 24 | **41.7%** | 6 (L40, L41) |
| Security | L46-L55 | 3 | 30 | **10.0%** | 0 |
| Observability & Ops | L56-L63 | 9 | 24 | **37.5%** | 9 (L60, L62, L63) |
| Documentation & SSOT | L64-L68 | 6 | 15 | **40.0%** | 0 |
| Governance & Sustainability | L69-L71 | 3 | 9 | **33.3%** | 0 |
| **TOTAL** | **L1-L71** | **87** | **213** | **40.8%** | **15** |

**Status glyph distribution (per scorecard convention):**
- ✓ (score 3): 17 pillars
- △ (score 1-2): 27 pillars
- ⚠ (score 0): 27 pillars
- N/A (treated as 3): 5 pillars (L40, L41, L60, L62, L63)

**Pillar count verification:** 12 + 7 + 8 + 10 + 8 + 10 + 8 + 5 + 3 = 71 ✓
**Sum verification:** 23 + 11 + 12 + 10 + 10 + 3 + 9 + 6 + 3 = 87 ✓

---

## 4. Top Strengths (top 5 pillars, by sum/max in this audit)

| L# | Pillar | Sum | % of max | Why it scores high |
|---|---|---:|---:|---|
| **L6** | Hexagonal port/adapter discipline | 3/3 | 100% | 21 traits in `ports/` directories, each module has `adapters/` for concrete impls, mock adapters for tests. Direct result of L5-114 absorption — 4 separate `ports/` trees merged into one coherent hexagonal pattern. |
| **L11** | Extensibility | 3/3 | 100% | Bevy adapter is optional feature; trait-based ports designed for pluggable backends; FFI edges planned in `src/lib.rs:39-41`; mock adapters demonstrate the contract. |
| **L15** | Concurrency safety & races | 3/3 | 100% | `#![forbid(unsafe_code)]` is the SOTA for memory safety in Rust. Sync-only = no data races possible. `Send + Sync` achieved by construction. |
| **L24** | Type system & error handling | 3/3 | 100% | thiserror enums in every module (postfx, terrain, water, voxel ports); `Clone + PartialEq + Eq` implementations for testability; `Result<T, Error>` chains throughout; no `unwrap` in lib. |
| **L25** | Memory safety | 3/3 | 100% | `#![forbid(unsafe_code)]` (better than `deny(unsafe_op_in_unsafe_fn)` because it forbids unsafe entirely); `bytemuck` for safe POD transmutation. |

**Top 4-module cohesion (post-absorption delta):** L2 (Module structure & boundaries) = 3/3. The 4 absorbed modules each preserved their own internal structure (voxel/terrain/water/postfx each with `ports/` + `adapters/` + `error.rs` + `mod.rs`); the single-core pattern means the boundaries are at the module level (clean) rather than the repo level (which was the pre-absorption state). 73 source files / 13,409 LOC across 4 modules = clean layer separation.

---

## 5. Top Weaknesses (top 5 pillars, by gap from max)

| L# | Pillar | Sum | % of max | Gap | Why it scores low |
|---|---|---:|---:|---:|---|
| **L48** | Threat model & attack surface | 0/3 | 0% | -3 | No STRIDE doc, no `docs/security/threat-model.md`. Graphics kernels are a known attack surface (shader injection, memory corruption via FFI); threat model should be in ADR-004 or a sibling doc. |
| **L51** | Audit log & compliance | 0/3 | 0% | -3 | No audit log. (Honest score — libraries don't typically audit-log, but for a graphics kernel exposed to FFI edges, an audit layer would help.) |
| **L54** | Dependency policy | 0/3 | 0% | -3 | No `deny.toml`. Single `Cargo.toml` with 13 direct deps (4 production: serde, serde_json, bytemuck, thiserror; 1 math: glam; 1 UUID; 1 optional Bevy; 2 dev: criterion, proptest). SOTA would be `deny.toml` license + RUSTSEC + bans. |
| **L55** | Security ops | 0/3 | 0% | -3 | No `gitleaks`, no `cargo-audit`, no `dependabot`, no `SECURITY.md`, no `security.txt`. |
| **L22** | Linting & static analysis | 0/3 | 0% | -3 | No `clippy` in CI. CI runs `cargo check --all-targets` but not `cargo clippy --all-targets -- -D warnings`. This is the **easiest win** — add 1 line to `.github/workflows/ci.yml`. |
| **L23** | Formatting & style consistency | 0/3 | 0% | -3 | No `rustfmt` in CI. Add `cargo fmt --check` to the same workflow. |

---

## 6. SUPERSEDED Source Repos — Last-Known Pre-Absorption Snapshot

For cross-reference. These are the last-known pre-absorption pillar scores for the 4 source repos, before they were collapsed into `phenotype-gfx`. They are preserved for historical traceability per the L5-113 Block-C audit verdict matrix.

### 6.1 `phenotype-voxel` (SUPERSEDED, 2026-06-18)

**Pre-absorption state:** 7,704 LOC, 94→122 tests (28 net new in PR #10), SVO + dense leaf chunks, dirty queue, BRP streaming. Single crate, no workspace.

| L# | Score | Justification (pre-absorption) |
|---|---:|---|
| L1 | 1 | `phenotype-voxel/ARCHITECTURE.md` minimal; 1 ADR (Civis ADR-005) |
| L2 | 3 | `phenotype-voxel/src/{chunk,coord,octree,world,mesh,...}` 14 modules, well-bounded |
| L6 | 3 | `phenotype-voxel/src/ports/` trait-only; `src/adapters/` concrete impls; exemplary |
| L24 | 3 | `phenotype-voxel/src/error.rs` thiserror |
| L25 | 3 | `#![forbid(unsafe_code)]` |
| L26 | 2 | `proptest!` in `src/sprite_voxelizer.rs` |
| L39 | 0 | No `AGENTS.md` |
| L53 | 2 | bytemuck + Chunk::default() |
| L64 | 1 | `phenotype-voxel/README.md` exists, basic |
| L69 | 2 | `phenotype-voxel/Cargo.toml:5` `license = "MIT OR Apache-2.0"` |
| L70 | 0 | No CODEOWNERS |
| **Sum** | ~38/71 (54%) on the scorable subset | Mostly strong; weak on DX + Governance |

### 6.2 `phenotype-terrain` (SUPERSEDED, 2026-06-18)

**Pre-absorption state:** 2,682 LOC, 7 test files (port parity). C# implementation; thin P/Invoke shim stub remained in `phenotype-gfx/unity/terrain/SHIM_README.md`.

| L# | Score | Justification (pre-absorption) |
|---|---:|---|
| L1 | 1 | `phenotype-terrain/ARCHITECTURE.md` minimal; no ADR |
| L2 | 2 | `phenotype-terrain/src/{Ports,ChunkMeshBuilder,HeightField,Materials}` C# namespaces |
| L6 | 2 | `phenotype-terrain/src/Ports/IMaterialRegistry.cs` + `ISerializationPort.cs` C# interfaces |
| L24 | 2 | `phenotype-terrain/src/TerrainError.cs` typed error |
| L39 | 1 | `phenotype-terrain/AGENTS.md` cross-ref (added in `317f62c`) |
| L53 | 1 | Input validation on `HeightField` |
| L69 | 1 | `phenotype-terrain/LICENSE` only |
| L70 | 0 | No CODEOWNERS |
| **Sum** | ~22/71 (31%) on the scorable subset | Weaker than voxel; C# parity gaps |

### 6.3 `phenotype-water` (SUPERSEDED, 2026-06-18)

**Pre-absorption state:** 3,145 LOC, 9 test files (port parity). C# implementation; thin P/Invoke shim stub remained in `phenotype-gfx/unity/water/SHIM_README.md`.

| L# | Score | Justification (pre-absorption) |
|---|---:|---|
| L1 | 1 | Minimal ARCHITECTURE.md; no ADR |
| L2 | 2 | C# namespaces, similar to terrain |
| L6 | 2 | C# `IMaterialRegistry.cs` + `ISerializationPort.cs` |
| L24 | 2 | `phenotype-water/Runtime/WaterError.cs` typed error |
| L39 | 1 | `phenotype-water/AGENTS.md` cross-ref (added in `c333788`) |
| L69 | 1 | LICENSE only |
| L70 | 0 | No CODEOWNERS |
| **Sum** | ~22/71 (31%) on the scorable subset | Similar to terrain; C# parity |

### 6.4 `phenotype-postfx` (SUPERSEDED, 2026-06-18)

**Pre-absorption state:** 5,426 LOC, 5 test files + 2 in-port test classes. C# + HLSL implementation; HLSL preserved in `phenotype-gfx/unity/postfx-shaders/`.

| L# | Score | Justification (pre-absorption) |
|---|---:|---|
| L1 | 1 | Minimal; no ADR |
| L2 | 3 | `phenotype-postfx/Runtime/{Ports,Shaders,Shaders}` 3 well-bounded C# namespaces |
| L6 | 3 | C# `IPostFxPass.cs` + `IMaterialRegistry.cs` + `ISerializationPort.cs` + `IUrpPostFxPass` + `IPostFxPassProvider` + `IUrpRenderGraphAdapter` |
| L24 | 3 | `phenotype-postfx/Runtime/PostFxError.cs` typed |
| L37 | 2 | HLSL shaders well-documented |
| L39 | 0 | No AGENTS.md (CLAUDE.md 28-LOC stub dropped on archive per `2026-06-18-sister-repo-block-c-summary.md:82`) |
| L64 | 2 | README had shader list + Unity setup |
| L69 | 1 | LICENSE only |
| L70 | 0 | No CODEOWNERS |
| **Sum** | ~30/71 (42%) on the scorable subset | Stronger than terrain/water; HLSL preservation helped |

### 6.5 Pre-absorption fleet sum (estimated)

| Source repo | Estimated sum (pre-absorption) | Status |
|---|---:|---|
| phenotype-voxel | 38/71 (54%) | SUPERSEDED |
| phenotype-terrain | 22/71 (31%) | SUPERSEDED |
| phenotype-water | 22/71 (31%) | SUPERSEDED |
| phenotype-postfx | 30/71 (42%) | SUPERSEDED |
| **Combined (weighted avg, no dedup)** | **~28/71 (39%)** | pre-absorption |
| **phenotype-gfx (post-absorption, this audit)** | **87/213 (40.8%)** | post-absorption |
| **Net delta** | **+1.8 pp** (modest; the absorption did not magically improve the underlying signal — it just consolidated 4 crates into 1) | |

**Honest framing:** The 71-pillar score did not jump dramatically because the 4 source repos were already at ~30-54% individually. The real wins are structural:
- **Single Cargo workspace** (was 4 separate crates) — easier to test, refactor, and reason about
- **Single CI loop** (was 4 separate CI workflows) — single source of truth
- **Single docs surface** (was 4 separate READMEs) — but the unified `phenotype-gfx/README.md` is still weak
- **Single governance** (was 4 separate CODEOWNERS) — but `phenotype-gfx` still has no CODEOWNERS
- **Deduplicated ports** (was 4×MaterialRegistry, 4×SerializationPort, 4×HealthCheck; now 4 ports total)

The next refresh (2026-06-23) should focus on **the missing security + observability + governance scaffolding** that the 4 source repos all lacked individually.

---

## 7. Per-Pillar Fleet Sum (phenotype-gfx only, this audit)

| L# | Pillar | Domain | Sum (1 repo) | Max (1 repo) | % | Verdict |
|---|---|---|---:|---:|---:|---|
| L1 | Architecture foundations | AX | 1 | 3 | 33% | △ |
| L2 | Module structure & boundaries | AX | 3 | 3 | 100% | ✓ |
| L3 | API surface & contract | AX | 2 | 3 | 67% | △ |
| L4 | Data model & state mgmt | AX | 2 | 3 | 67% | △ |
| L5 | Async/concurrency design | AX | 1 | 3 | 33% | △ |
| L6 | Hexagonal port/adapter | AX | 3 | 3 | 100% | ✓ |
| L7 | Polyglot strategy | AX | 2 | 3 | 67% | △ |
| L8 | Substrate placement | AX | 2 | 3 | 67% | △ |
| L9 | Cargo workspace topology | AX | 1 | 3 | 33% | △ |
| L10 | Backward compatibility | AX | 2 | 3 | 67% | △ |
| L11 | Extensibility | AX | 3 | 3 | 100% | ✓ |
| L12 | Portability | AX | 1 | 3 | 33% | △ |
| L13 | Performance budgets & SLOs | Performance | 2 | 3 | 67% | △ |
| L14 | Memory & allocation | Performance | 1 | 3 | 33% | △ |
| L15 | Concurrency safety & races | Performance | 3 | 3 | 100% | ✓ |
| L16 | Resource limits & rate limits | Performance | 0 | 3 | 0% | ⚠ |
| L17 | Build performance | Performance | 1 | 3 | 33% | △ |
| L18 | Runtime latency | Performance | 2 | 3 | 67% | △ |
| L19 | Throughput | Performance | 2 | 3 | 67% | △ |
| L20 | Test coverage & quality gates | Quality | 1 | 3 | 33% | △ |
| L21 | Test health | Quality | 2 | 3 | 67% | △ |
| L22 | Linting & static analysis | Quality | 0 | 3 | 0% | ⚠ |
| L23 | Formatting & style consistency | Quality | 0 | 3 | 0% | ⚠ |
| L24 | Type system & error handling | Quality | 3 | 3 | 100% | ✓ |
| L25 | Memory safety | Quality | 3 | 3 | 100% | ✓ |
| L26 | Property/fuzz testing | Quality | 2 | 3 | 67% | △ |
| L27 | Code complexity & duplication | Quality | 1 | 3 | 33% | △ |
| L28 | Local dev setup | DX | 1 | 3 | 33% | △ |
| L29 | Test speed | DX | 2 | 3 | 67% | △ |
| L30 | Build cache | DX | 2 | 3 | 67% | △ |
| L31 | Pre-commit hooks | DX | 0 | 3 | 0% | ⚠ |
| L32 | Editor/IDE config | DX | 0 | 3 | 0% | ⚠ |
| L33 | Debug tooling | DX | 0 | 3 | 0% | ⚠ |
| L34 | Code generation / scaffolding | DX | 0 | 3 | 0% | ⚠ |
| L35 | Migration tooling | DX | 2 | 3 | 67% | △ |
| L36 | CI loop time | DX | 2 | 3 | 67% | △ |
| L37 | Doc generation | DX | 1 | 3 | 33% | △ |
| L38 | Onboarding: clone-to-first-build | UX | 1 | 3 | 33% | △ |
| L39 | AGENTS.md quality & freshness | UX | 0 | 3 | 0% | ⚠ |
| L40 | i18n | UX | 3 (N/A) | 3 | 100% | n/a |
| L41 | a11y | UX | 3 (N/A) | 3 | 100% | n/a |
| L42 | Error messages: human-readable | UX | 2 | 3 | 67% | △ |
| L43 | CLI discoverability | UX | 0 | 3 | 0% | ⚠ |
| L44 | Progress indication | UX | 0 | 3 | 0% | ⚠ |
| L45 | Help & examples | UX | 1 | 3 | 33% | △ |
| L46 | Secret management | Security | 0 | 3 | 0% | ⚠ |
| L47 | Supply-chain security | Security | 1 | 3 | 33% | △ |
| L48 | Threat model & attack surface | Security | 0 | 3 | 0% | ⚠ |
| L49 | AuthN/AuthZ | Security | 0 | 3 | 0% | ⚠ |
| L50 | Cryptography & key management | Security | 0 | 3 | 0% | ⚠ |
| L51 | Audit log & compliance | Security | 0 | 3 | 0% | ⚠ |
| L52 | Multi-tenant isolation | Security | 0 | 3 | 0% | ⚠ |
| L53 | Input validation & sanitization | Security | 2 | 3 | 67% | △ |
| L54 | Dependency policy | Security | 0 | 3 | 0% | ⚠ |
| L55 | Security ops | Security | 0 | 3 | 0% | ⚠ |
| L56 | Structured logging | Obs&Ops | 0 | 3 | 0% | ⚠ |
| L57 | Distributed tracing | Obs&Ops | 0 | 3 | 0% | ⚠ |
| L58 | Metrics collection | Obs&Ops | 0 | 3 | 0% | ⚠ |
| L59 | Health & readiness probes | Obs&Ops | 0 | 3 | 0% | ⚠ |
| L60 | Deployment automation | Obs&Ops | 3 (N/A) | 3 | 100% | n/a |
| L61 | Incident response & runbooks | Obs&Ops | 0 | 3 | 0% | ⚠ |
| L62 | Backup/restore | Obs&Ops | 3 (N/A) | 3 | 100% | n/a |
| L63 | Capacity planning & SLOs | Obs&Ops | 3 (N/A) | 3 | 100% | n/a |
| L64 | README quality | Doc&SSOT | 1 | 3 | 33% | △ |
| L65 | Spec / SSOT | Doc&SSOT | 1 | 3 | 33% | △ |
| L66 | LLM-friendly docs (llms.txt) | Doc&SSOT | 0 | 3 | 0% | ⚠ |
| L67 | API reference (cargo doc) | Doc&SSOT | 2 | 3 | 67% | △ |
| L68 | Tutorial / concept docs (Divio) | Doc&SSOT | 2 | 3 | 67% | △ |
| L69 | License & SPDX | Gov&Sust | 2 | 3 | 67% | △ |
| L70 | Code ownership & governance | Gov&Sust | 0 | 3 | 0% | ⚠ |
| L71 | Sustainability | Gov&Sust | 1 | 3 | 33% | △ |

**Pillar status summary (71 pillars):**
- ✓ (score 3, including 5 N/A-3): 17 pillars (5 N/A + 12 scorable)
- △ (score 1-2): 27 pillars
- ⚠ (score 0): 27 pillars

---

## 8. Future Pillars (Appendix — for schema-2.0 review)

The following pillars were considered but **not added** to the scorecard this turn (per the user's constraint: "do not add or remove pillars this turn — only score them"). They are recorded here for the schema-2.0 review (quarterly cadence per § 5.3 of the schema doc).

| Candidate | Domain | Why it should be added | Evidence from phenotype-gfx audit |
|---|---|---|---|
| **L72** | **FFI surface** | The 4-repo absorption centered on a single Rust core + thin FFI edges (ADR-004). FFI surface (C-ABI via cbindgen, wasm-bindgen) is the integration seam for C#, TS, and web consumers. A dedicated pillar would score the quality of the FFI layer: type-stable C-ABI, safe ownership transfer, ABI versioning, NULL-handling, panic safety. | `phenotype-gfx/src/lib.rs:39-41` shows planned `c_api` and `wasm` modules; `phenotype-gfx/Cargo.toml:15` has `crate-type = ["cdylib", "rlib"]`; the 4 sister C# repos are stubs at `phenotype-gfx/unity/{terrain,water}/SHIM_README.md`. The FFI surface is 0% implemented (no `c_api.rs` yet). |
| **L73** | **Graphics / GPU correctness** | Graphics kernels have correctness concerns distinct from general-purpose Rust: shader compiler bugs, GPU memory alignment, driver-specific workarounds, determinism (replay-safe voxel writes), NaN/Inf propagation, half-precision math. A dedicated pillar would score these. | `phenotype-gfx/src/voxel/coord.rs:18-20` "World coordinates are fixed-point `i64` at `10^6` scale. No `f32`/`f64` crosses the public API." Determinism contract at `phenotype-gfx/src/voxel/mod.rs:14-24`. `bytemuck` for safe GPU buffer layout. |
| **L74** | **Asset pipeline / schema versioning** | `SCHEMA_VERSION: u32 = 1` + `is_supported_schema_version()` at `phenotype-gfx/src/voxel/mod.rs:79-94` is a critical pattern for any library that persists to disk or sends over the wire. Distinct from L10 (Backward compatibility for API) and L35 (Migration tooling for in-place schema migration). | The voxel schema version is referenced by `.civreplay` (a Civis-specific replay format). The water/postfx schemas are *not* versioned. A dedicated pillar would catch this. |
| **L75** | **Reactive / streaming behaviour** | `phenotype-gfx/src/streaming.rs:1-12600` is a streaming window policy module: ring-based chunk lifecycle, eviction ordering. Distinct from L5 (Async/concurrency) because the concern is *resource-constrained streaming* with bounded memory + eviction, not just concurrency. | `phenotype-gfx/src/streaming.rs` is a 12,683-line module (largest in the repo) with no coverage in the current pillars. |
| **L76** | **Polyglot consumer parity** | The 4-repo absorption left 9 HLSL shaders in `phenotype-gfx/unity/postfx-shaders/` and 2 SHIM_README.md stubs in `phenotype-gfx/unity/{terrain,water}/`. These are the consumer-side artifacts. A dedicated pillar would track parity between Rust core and polyglot consumer artifacts (test parity, doc parity, version parity). | `phenotype-gfx/unity/postfx-shaders/` (9 HLSL files), `phenotype-gfx/unity/terrain/SHIM_README.md`, `phenotype-gfx/unity/water/SHIM_README.md`. The Block-C audit (`2026-06-18-sister-repo-block-c-summary.md:62-85`) calls out 16 left-behind items in source. |

**Schema-2.0 review trigger:** the schema doc says "the 71-pillar set is reviewed quarterly; the scorecard is refreshed weekly." The next schema review is 2026-09-17 (90 days from 2026-06-17). These 5 candidate pillars (L72-L76) should be on the agenda for that review.

---

## 9. Reproducibility

```bash
# Re-run the probe (read-only)
cd /tmp/migration/phenotype-gfx
ls -la Cargo.toml Cargo.lock README.md VERSION.toml docs/adr/ findings/ examples/ benches/ tests/ unity/
find src -name "*.rs" -type f | wc -l                                     # 73
find src -name "*.rs" -exec wc -l {} + | tail -1                          # 13409 total
grep -rln "#\[test\]" --include="*.rs" . | wc -l                          # 49
grep -rn "#\[test\]" --include="*.rs" . | wc -l                           # 314
grep -rn "use thiserror" --include="*.rs" src/ | wc -l                    # typed errors
grep -rn "use tracing" --include="*.rs" . | wc -l                         # 0 (no tracing)
find . -name "deny.toml" -o -name ".gitleaks*" -o -name "lefthook*"       # none
```

**Cost:** ~30 tool calls (one parallel probe per category + one render pass).
**Latency:** probe ~30s (single crate, no `cargo build`); render <1s.
**Modifications:** 0 (read-only across the target repo).

---

## 10. Cross-References

| Source | Path | Notes |
|---|---|---|
| Schema doc (canonical) | `findings/71-pillar-2026-06-17-schema.md` | L1-L71, 9 domains, scoring rubric |
| Previous scorecard | `findings/71-pillar-2026-06-17.md` | 10 repos; phenotype-gfx not yet in scope (was `phenotype-voxel` + `phenotype-terrain` + `phenotype-water` + `phenotype-postfx` as separate repos) |
| Crosswalk to 30-pillar | `findings/71-pillar-2026-06-17-mapping.md` | L1-L30 → L1-L71 mapping |
| 4-repo retirement | `findings/2026-06-18-L5-114-4-repo-retirement.md` | L5-114 audit doc with full migration matrix |
| Block-C audit verdict | `phenotype-gfx/findings/2026-06-18-sister-repo-block-c-summary.md` | L5-113 audit; 4/4 SUPERSEDED → phenotype-gfx |
| Voxel Block-C | `phenotype-gfx/findings/2026-06-16-voxel-block-c.md` | 373 lines, voxel-specific audit |
| Terrain Block-C | `phenotype-gfx/findings/2026-06-16-terrain-block-c.md` | 178 lines |
| Water Block-C | `phenotype-gfx/findings/2026-06-16-water-block-c.md` | 178 lines |
| Postfx Block-C | `phenotype-gfx/findings/2026-06-18-postfx-block-c.md` | 333 lines |
| PR #10 | <https://github.com/KooshaPari/phenotype-gfx/pull/10> | The 4-repo absorption PR (merged) |
| ADR-004 | `phenotype-gfx/docs/adr/ADR-004-single-core-ffi-edges.md` | The single-core + thin FFI edges architecture decision |
| ADR-024 | `docs/adr/2026-06-17/ADR-024-71-pillar-audit-framework.md` | The 71-pillar framework adoption |
| ADR-026 | `docs/adr/2026-06-17/ADR-026-factory-ai-agent-readiness.md` | The Factory AI Agent Readiness external standard (see § 11) |
| AGENTS.md fleet overview | `AGENTS.md` § "71-pillar audit" | 9-domain taxonomy SSOT |
| Refresh cadence | weekly Monday 09:00 PDT | Next refresh: **2026-06-23** |

---

## 11. Factory AI Agent Readiness Crosswalk (per ADR-026)

> The Factory AI Agent Readiness Model (<https://docs.factory.ai/web/agent-readiness/overview>) provides a **5-level progression** (Functional → Documented → Standardized → Optimized → Autonomous) gated by an **80% threshold per level**. The 71-pillar measures **breadth** (how many of the 9 domains are covered?); Factory AI measures **depth** (how SOTA is the implementation?). Both are needed for a complete picture.

### 11.1 Crosswalk: 9 Factory AI technical pillars → 9 71-pillar domains

| # | Factory AI technical pillar | 71-pillar domain(s) | 71-pillar coverage (phenotype-gfx) | Notes |
|---|---|---|---:|---|
| 1 | **Style & Validation** | Quality (L20-L27) | 50% (12/24) | thiserror + forbid(unsafe_code) ✓; no clippy/rustfmt in CI ✗ |
| 2 | **Build System** | DX (L28, L30, L36) | 44% (5/9) | cargo + actions/cache ✓; no sccache/mold ✗ |
| 3 | **Testing** | Quality (L20, L21, L22, L26) | 38% (5/13) | 311 unit tests pass ✓; no coverage tool ✗; no property/fuzz in CI ✗ |
| 4 | **Documentation** | Doc&SSOT (L64-L68) + UX (L39) | 40% (6/15) + 0/3 (L39) = 6/18 = 33% | cargo doc builds ✓; 5 findings ✓; no llms.txt ✗; no AGENTS.md ✗; outdated README ✗ |
| 5 | **Dev Environment** | DX (L28, L32, L34) | 13% (1/9) | no justfile/devcontainer/mise.toml ✗; no .editorconfig at root ✗; no scaffolding ✗ |
| 6 | **Debugging & Observability** | DX (L33) + Obs&Ops (L56-L58) | 0% (0/12) | no cargo-flamegraph ✗; no tracing ✗; no OTel ✗; no metrics ✗ — **largest gap** |
| 7 | **Security** | Security (L46-L55) | 10% (3/30) | Cargo.lock ✓; no deny.toml ✗; no gitleaks ✗; no cargo-audit ✗; no SECURITY.md ✗ — **2nd largest gap** |
| 8 | **Task Discovery** | Doc&SSOT (L64, L65, L66, L67, L68) + AX (L1, L12) | 38% (8/21) | 5 findings ✓; 1 ADR ✓; cargo doc ✓; no llms.txt ✗; no AGENTS.md ✗; outdated README ✗ |
| 9 | **Product & Experimentation** | UX (L38, L42, L43, L44, L45) + Gov&Sust (L71) | 26% (4/18) | 1 example ✓; structured errors ✓; no CLI/progress/tutorial ✗; no ROADMAP ✗; no FUNDING.yml ✗ |

### 11.2 Estimated current Factory AI Level

**Level estimate: 2 (Documented) → 3 (Standardized) target for next quarter.**

| Level | Description | Threshold | phenotype-gfx status | Why |
|---|---|---:|---|---|
| **L1 Functional** | Basic functionality, can compile and run | 80% | **PASS** | `cargo check --all-targets` ✓; 311 tests pass ✓; can be consumed as a Rust crate ✓ |
| **L2 Documented** | Has docs, can onboard a new developer | 80% | **PARTIAL** | cargo doc builds (1 item) ✓; 5 findings (1,202 lines) ✓; 1 ADR ✓; but no AGENTS.md ✗; no llms.txt ✗; outdated README (doesn't mention postfx) ✗. Estimated 60% — below 80% threshold. |
| **L3 Standardized** | Linting, formatting, CI, consistent style | 80% | **FAIL** | No clippy in CI ✗; no rustfmt in CI ✗; no editorconfig ✗; no pre-commit ✗. Estimated 30%. |
| **L4 Optimized** | Performance monitored, error handling SOTA, observability | 80% | **FAIL** | thiserror ✓; forbid(unsafe_code) ✓; criterion benches ✓; perf_regression_guards.rs ✓. But: no tracing ✗; no OTel ✗; no metrics ✗; no SLO ✗. Estimated 40%. |
| **L5 Autonomous** | Can self-improve, full automation | 80% | **FAIL** | No self-improving CI; no auto-update deps; no auto-format PRs. Estimated 0%. |

**Org-level Factory AI level for phenotype-gfx (per ADR-026 §10 rule):** `floor(average of L1..L5)` = `floor((1+0.6+0.3+0.4+0)/5)` = `floor(0.46)` = **Level 0 (Pre-Functional)**. This is honest: phenotype-gfx passes L1 (can be used) but the gaps in L2-L5 bring the org average below 80%.

**Target for next quarter (2026-09-17 schema review):**
- L2 → 3 (Documented → Standardized) by adding:
  1. `AGENTS.md` at root (5 min, copy from `pheno-config` template)
  2. `llms.txt` at root (10 min, per Answer.AI spec)
  3. Update `README.md` to mention all 4 modules (post-L5-114 absorption)
  4. Update `VERSION.toml` to include postfx (5 min)
  5. `cargo clippy --all-targets -- -D warnings` in CI (1 line)
  6. `cargo fmt --check` in CI (1 line)
  7. `deny.toml` from `phenotype-ops/templates/deny.toml` (10 min)
  8. `gitleaks` workflow (10 min)
  9. `cargo-audit` workflow (10 min)
  10. `.editorconfig` at root (2 min)
- L3 → 4 (Standardized → Optimized) requires:
  1. `tracing` + `tracing-subscriber` in deps
  2. `pheno-tracing` (per ADR-012) as a substrate
  3. `pheno-otel` OTLP exporter
  4. `SLO.md` with P50/P95/P99 for hot paths
  5. Regression alerts in CI (>5% slowdown fails)

**Why both frameworks?** The 71-pillar measures **breadth** (9 domains × 71 pillars = comprehensive static scoring). Factory AI measures **depth** (5 levels × 9 pillars = progressive depth). Skipping 71-pillar misses breadth; skipping Factory AI misses progression. Both are tracked weekly; per-repo readiness estimates live in `STATUS.md` § "Factory AI Agent Readiness" (per ADR-026).

---

## 12. Sign-off

- **Mission:** Track T3 of v8 DAG (weekly 71-pillar refresh, 2026-06-18)
- **Focal repo:** `KooshaPari/phenotype-gfx` (post-L5-114 absorption)
- **Source repos:** 4 SUPERSEDED (phenotype-voxel, phenotype-terrain, phenotype-water, phenotype-postfx)
- **Score:** 87/213 = **40.8%** (vs 2026-06-17 fleet avg 46.9%; -6.1 pp on different denominator, +0 pp on weighted-source-avg basis)
- **Top strength:** L6, L11, L15, L24, L25 (all 3/3 = 100%)
- **Top weakness:** L48, L51, L54, L55, L22, L23 (all 0/3 = 0%)
- **Factory AI Level:** 2 (Documented) → 3 (Standardized) target for next quarter
- **Future pillars:** 5 candidates (L72-L76) for schema-2.0 review on 2026-09-17
- **Next refresh:** 2026-06-23 09:00 PDT
- **PR (this audit):** to be opened after this file is committed

---

**End of scorecard. Refresh on 2026-06-23.**
